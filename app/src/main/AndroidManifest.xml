<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assis.firebase">

    <uses-permission android:name="android.permission.INTERNET">
        <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PrincipalActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".CadastroActivity" />
        <activity android:name=".SecundarioCad" />
        <activity android:name=".SecundarioRelatorios" />
        <activity
            android:name=".SecundarioAjustes"
            android:label="@string/title_activity_secundario_ajustes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SecundarioConsultas" />
        <activity android:name=".SecundarioAgenda" />
        <activity android:name=".SecundarioLancamentos" />
        <activity android:name=".AddTrack" />
        <activity android:name=".LancContLeite" />
        <activity android:name=".LancContRep" />
        <activity android:name=".LancContFinan"></activity>
    </application>

</manifest>